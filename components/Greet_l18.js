import { StyleSheet, Text, View } from 'react-native'

const Greet_l18 = ({name}) => {
  return (
    <View>
      <Text>Hello, {name}</Text>
    </View>
  )
}

export default Greet_l18

const styles = StyleSheet.create({})